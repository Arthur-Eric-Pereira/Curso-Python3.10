# Biblioteca: math
#   import ...
#   from ... import ...
'''

import math:
    math.ceil()             -> arredonda os valores para cima
    math.floor()            -> arredonda os valores para baixo
    math.trunc()            -> elimina os valores depois da vírgula
    math.pow()              -> potência, semelhante a (**)
    math.sqrt()             -> calcula a raiz quadrada
    math.factorial()        -> cálculos fatoriais
    math.hypot()            -> calcula a hipotenusa

    '''

# Biblioteca: random            -> "tuplas, listas, dicionários" <-
#   import ...
#   from ... import ...
'''

import random:
    random.choice()            -> retorna apenas 1 elemento dentro da Lista
    random.choices()           -> retorna uma quantidade "K" de elementos da Lista, podendo ser eles repetidos! EX: (lista, k=5)
    random.sample()            -> retorna uma quantidade "K" de elementos da Lista, sem repetir os elementos! EX: (lista, k=7)
    random.shuffle()           -> embaralha a Lista
    random.randint()           -> define um parametro (1 até 10)-por exemplo, e sortea um numero dentro do parametro
    '''